# Implementation Plans

Generated by the improve skill on 2026-07-03 against commit `0b0d4bb` (v0.2.4).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Selection note: the operator was asked which findings to plan and did not
respond in-session; the advisor's recommended default was applied — the top 9
findings by leverage plus the test/CI baseline. The remaining audited findings
are listed under "Audited but not planned" so they aren't lost or re-audited.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Stop minting a new DEK on keychain read errors | P1 | S | — | DONE (branch advisor/001-keyring-dek-error-handling) |
| 002 | Test + CI baseline (Vitest, typecheck, GitHub Actions) | P1 | M | — | DONE (branch advisor/002-test-ci-baseline) |
| 003 | Touch ID gate test seam + regression tests | P1 | M | — | DONE (branch advisor/003-biometric-gate-test-seam) |
| 004 | Surface Touch ID failures to the user | P1 | S | — | DONE (branch advisor/004-touchid-failure-feedback; note: plan-specified copy uses an em dash — owner may want a reword) |
| 005 | Keep summon panel open during Touch ID prompt | P1 | S | 004 | DONE (branch advisor/005-summon-blur-guard, includes 004; guarded 3 prompting sites) |
| 006 | Sweep decrypted temp files (quickboard-drag/-clip) | P1 | M | — | DONE (branch advisor/006-sweep-drag-temp-dirs; plan amended in-flight: quickboard-clip uses subdir sweeper) |
| 007 | Encrypt clipboard history at rest | P2 | M-L | 002 | DONE (branch advisor/007-encrypt-clipboard-history, includes 002; clips.enc via DEK, one-shot localStorage migration) |
| 008 | Stop refetching the whole board on every mutation | P2 | M | 002 | DONE (branch advisor/008-targeted-board-updates, includes 002; note: src/lib/commit-tray.ts found to be dead code) |
| 009 | Cache image thumbnail data-URLs | P2 | S | — | DONE (branch advisor/009-image-thumbnail-cache; drag.ts kept uncached by design) |
| 010 | Tray discoverability (onboarding beat + header button) | P2 | M | — | DONE (branch advisor/010-tray-discoverability; visual pass pending — executor could not run the GUI) |
| 011 | Honest backup: export to file + import | P2 | M | — | DONE (branch advisor/011-honest-backup-export-import; backup.test.ts needs the 002 vitest branch merged to run) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **005 after 004**: both edit the same `catch` blocks in `SummonPanel.tsx`;
  004 defines the cancel-vs-failure branching 005's reasoning relies on.
- **007 after 002**: plan 002's clipboard.ts characterization tests define the
  behavior the persistence swap must preserve — do not land 007 without them.
- **008 after 002**: needs the Vitest runner for its board-sync tests.
- 001, 003, 006 are independent and can run in parallel worktrees; 001 is the
  most urgent fix in the set (silent total-data-loss path).
- 008 + 009 compound: full perf win requires both.

## Additional findings — resolved 2026-07-03 (post-merge hardening pass)

All plans 001–011 are merged into main. The following unplanned findings were
then fixed directly (owner-authorized):

- Store mutex poisoning: every `store.lock().unwrap()` now recovers via
  `PoisonError::into_inner` — one panic no longer bricks all commands.
- 140ms paste sleep: replaced with a frontmost-PID poll (20ms steps, 400ms
  cap, 30ms settle) with the old delay as fallback when no PID was captured.
  **Needs a manual paste smoke-test** (timing-sensitive).
- Idle clipboard poll: with history off, the 600ms thread wakes but performs
  zero AppKit/pasteboard calls; re-enable still skips the pre-existing copy.
- Store schema migration: now covered by a legacy-table test.
- Toast container: `role="status"` + `aria-live="polite"`.
- three.js/@shadergradient: fully lazy (baker canvas split into its own
  chunk, ShaderHeader lazy in all three modals) — the 1.1MB GL chunk left
  the main entry; verified via build output.
- `csp: null` → restrictive CSP in tauri.conf.json. **Needs a runtime
  smoke-test in all three windows** (one-line revert if anything breaks).
- ESLint flat-config baseline (`pnpm lint`, in CI): 0 errors; the react-hooks
  v7 opinionated rules are warnings pending refactors.
- release.sh now aborts on version drift across the three manifests.
- CONTEXT.md + docs/adr/ created (CLAUDE.md/AGENTS.md refs now resolve).
- Dead `src/lib/commit-tray.ts` removed; 004's em-dash copy reworded.
- ⌘N hint: already present on the Sidebar "New item" button — stale finding.

## Deliberately not done (needs owner decision / prerequisite work)

- TrayDock decomposition, ItemCard/ItemRow dedupe, commands.rs module split —
  large behavior-preserving refactors; land characterization tests first.
- ItemsContext split (per-keystroke re-renders) — meaningful surgery across
  18 consumers; do alongside the TrayDock work.
- Password-copy heuristics beyond the concealed-type markers — false-positive
  trade-off is a product call (history is opt-in and now encrypted).
- Prettier / formatting pass — pure churn; adopt only if wanted.

## Audited but not planned (original list, superseded by the section above)

- Password-manager skip relies on voluntary NSPasteboard concealed markers
  (`commands.rs:493-507`) — partially mitigated by plan 007; heuristics risk
  false positives, owner call.
- `csp: null` + IPC commands taking arbitrary paths (`tauri.conf.json:58`,
  `read_image_as_data_url` et al.) — defense-in-depth; needs a careful CSP
  + path-scoping pass across three windows.
- Store mutex `.unwrap()` poisoning (~30 sites in `commands.rs`) — S fix,
  centralize in a lock helper.
- Fixed 140ms sleep before synthetic ⌘V (`commands.rs:169-189`) — replace
  with frontmost-app polling; timing-sensitive, needs manual testing.
- ItemsContext split (all 18 consumers re-render per keystroke,
  `items-store.tsx:120-142`).
- Lazy-load three.js/shadergradient (`main.tsx:16`) — S effort bundle win.
- Idle clipboard poll does AppKit calls every 600ms when disabled
  (`commands.rs:473-490`).
- Toast `aria-live` missing (`Toast.tsx:35`) — S effort a11y fix.
- ⌘N has no on-screen hint; no shortcuts cheatsheet.
- TrayDock decomposition (~1,580 lines), ItemCard/ItemRow dedupe,
  commands.rs module split — do TrayDock only after characterization tests.
- ESLint/Prettier setup; release.sh version-sync check; store-migration test;
  fix CLAUDE.md/AGENTS.md pointing at nonexistent `CONTEXT.md` + `docs/adr/`.

## Direction options (owner strategy calls, from the audit)

- **DIR-01 Customizable global hotkeys** — ⌥Space/⌥⇧Space hardcoded in
  `lib.rs:91-93`; table-stakes for a launcher. Effort M.
- **DIR-02 Passphrase-wrapped full encrypted archive** (files + confidential
  included) with import/merge — supersedes plan 011's UX; prerequisite for
  any sync story. Effort L, design spike first.
- **DIR-03 "?" shortcuts overlay** — multiplies plan 010. Effort M.
- **DIR-04 Multi-device sync spike** — investigation only, after DIR-02.

## Findings considered and rejected

- "Store rewrites/re-encrypts the whole file per save" — false; SQLite with
  per-row updates and per-item AES-GCM (`store.rs:139-341`).
- "Images stored as base64 in the DB" — false; encrypted blobs on disk
  (`blobs.rs`); the base64 cost is at the IPC read boundary (plan 009).
- "Startup does heavy work" — false; setup is keyring + SQLite open + window
  wiring (`lib.rs:66-235`).
- "Touch ID gate bypassable via some IPC command" — none found; every egress
  path gates server-side and `list_items` never ships decrypted bodies.
- "No clipboard search in tray" — false; search + type/source filters exist
  (`TrayDock.tsx:655-676`).
- Reduced-motion, modal focus traps, empty states, updater error handling —
  all verified well-handled; not findings.
- "Remotion devDeps are shipped bloat" — false; dev-only marketing tooling.
- Loosening exact-pinned dependency versions — explicitly against owner
  policy; never suggest.
- staged-filename path traversal — sanitized (`write_staged`/`stage_text_file`
  strip separators).
- AES-GCM nonce reuse / missing auth tag — false; fresh 96-bit nonce per
  encrypt, tag verified (`crypto.rs`).

## Audit coverage note

Standard-depth audit: all nine categories over `src/` and `src-tauri/src/`.
Not audited: `src/remotion/` (marketing videos), `docs/prototypes/`,
`scripts/release.sh` beyond a version-sync skim, and no runtime/dynamic
testing was performed.
